import 'package:flutter/material.dart';

class KanakColors {
  // Brand Base Colors
  static const Color primary = Color(0xFF25326A);          // Deep Indigo
  static const Color primaryLight = Color(0xFF3B4D9A);     // Lighter Indigo
  static const Color primaryContainer = Color(0xFF1E2855); // Recessed Indigo for dark modules
  
  static const Color secondary = Color(0xFF00D1C1);        // Vibrant Teal Accent
  static const Color secondaryDark = Color(0xFF00B0A3);    // Muted Teal
  static const Color secondaryContainer = Color(0xFF57FAE9); // Soft Glow Teal
  
  // Surfaces & Backgrounds
  static const Color background = Color(0xFFF8F9FF);       // Soft Slate-Blue Base
  static const Color surface = Color(0xFFFFFFFF);          // High elevation white cards
  static const Color surfaceDim = Color(0xFFCBDBF5);       // Shaded details base
  
  static const Color surfaceContainerLowest = Color(0xFFFFFFFF);
  static const Color surfaceContainerLow = Color(0xFFEFF4FF);
  static const Color surfaceContainer = Color(0xFFE5EEFF);
  static const Color surfaceContainerHigh = Color(0xFFDCE9FF);
  static const Color surfaceContainerHighest = Color(0xFFD3E4FE);

  // Typography Neutrals
  static const Color textPrimary = Color(0xFF0B1C30);      // High-contrast slate-indigo
  static const Color textSecondary = Color(0xFF454650);    // Secondary titles
  static const Color textMuted = Color(0xFF767681);        // Muted labels / borders
  static const Color outline = Color(0xFFC6C5D1);          // Borders and outlines
  
  // Semantic Indicators
  static const Color success = Color(0xFF006A62);          // Success dark teal
  static const Color successBg = Color(0xFFE0FDF7);        // Success soft backdrop
  
  static const Color error = Color(0xFFBA1A1A);            // Error crimson
  static const Color errorBg = Color(0xFFFEECEB);          // Error soft backdrop

  static const Color warning = Color(0xFFD97706);          // Warning Amber
  static const Color warningBg = Color(0xFFFEF3C7);        // Warning soft backdrop
}
